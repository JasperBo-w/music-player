# ==========================================================================
# 部署排除清单
# --------------------------------------------------------------------------
# wrangler pages deploy . 会把本目录所有文件一起上传，包括文档和工具脚本 ——
# 那些不该出现在公开站点上（DEPLOY.md 里甚至写了你的存储桶命名约定）。
# 这个文件让 Pages 在上传阶段就把它们剔除，比事后手动整理目录可靠。
#
# 语法与 .gitignore 相同，路径相对于本目录。
# ==========================================================================

# 文档：只给维护者看
README.md
DEPLOY.md
RELEASE.md

# 本地预览用的服务器，线上不需要
.preview-server.cjs

# 截图处理脚本，线上不需要
tools/

# Wrangler 自己的配置和部署产物
wrangler.toml
.wrangler/
node_modules/

# 编辑器 / 系统残留
.DS_Store
Thumbs.db
*.log
