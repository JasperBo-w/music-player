# ==========================================================================
# Cloudflare Pages 重定向配置
# --------------------------------------------------------------------------
# 语法：<来源> <目标> <状态码>
#   - 以 / 开头的来源是本站路径；以 http 开头的是完整 URL（可以做外跳）。
#   - 301 永久重定向会被浏览器缓存，改主意了不好回退；这里统一用 302，
#     等你确定短链不再变动，再改成 301 更好（省一次请求）。
# ==========================================================================

# 固定下载短链：以后换版本只改 R2 里的文件名，这个链接永远不用动。
# 也可以用 Cloudflare 的 Bulk Redirects / Redirect Rules 做到不改代码就换目标，
# 那时把这条删掉即可（避免两条规则打架）。
# /dl/latest         https://pub-REPLACE-WITH-YOUR-BUCKET.r2.dev/latest/MusicPlayer-Setup.exe   302

# 保留旧入口，避免有人收藏了带 .html 的地址
/index.html         /        302
